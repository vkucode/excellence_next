// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // output: "export",
  images: {
    domains: ["excellencebtp.com"],
  },
};

module.exports = nextConfig;
